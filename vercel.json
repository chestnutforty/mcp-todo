{
  "installCommand": "git config --global url.\"https://${GITHUB_TOKEN}@github.com/\".insteadOf \"https://github.com/\" && uv sync --no-dev",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
